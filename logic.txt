

oldIndex

for move R
[y][x] add 1 to x

for move L
[y][x] subtract 1 to x

for move D
[y][x] add 1 to y

for move U
[y][x] subtract 1 to y



if newIndex [y][x]  
    if y<0, y > 2  -- game over
    if x < 0, x>2 -- game over

    if [0][2] = game over 
    if [1][1] = game over
    if [2][1] = Win found hat

 otherwise 
    if [0][1] or [1][0] or [1][2] or [2][0] or [2][2]

    then newIndex should save a star character

    when game over, reset the map